# Examples

This repository currently includes a minimal validation project rather than gameplay scenes.

Open `project.godot` to verify that `ability_combat`, `modular_data`, and `godot_events` load together. Add local example scenes here when demonstrating a specific integration.
